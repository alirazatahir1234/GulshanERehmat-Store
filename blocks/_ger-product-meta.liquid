{%- doc -%}
  Brand line for collection product cards (vendor).
{%- enddoc -%}

{% assign product = closest.product %}

{% if product != blank and product.vendor != blank %}
  <p class="ger-product-brand">{{ product.vendor }}</p>
{% endif %}

{% schema %}
{
  "name": "GER Product meta",
  "tag": null,
  "settings": []
}
{% endschema %}
